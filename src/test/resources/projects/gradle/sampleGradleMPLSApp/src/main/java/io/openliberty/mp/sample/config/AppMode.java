package io.openliberty.mp.sample.config;

/**
 * Application mode enumeration used to demonstrate MicroProfile Config
 * property validation with enum types in LSP4MP.
 */
public enum AppMode {
    DEVELOPMENT,
    PRODUCTION,
    TEST
}
